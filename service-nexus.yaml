apiVersion: v1
kind: Service
metadata:
  name: nexus
  namespace: bootcamp
  labels:
    app: nexus
spec:
  type: ClusterIP
  selector:
    app: nexus
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
